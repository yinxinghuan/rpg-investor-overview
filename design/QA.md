# 候选页面检查

2026-09-18，使用只提供 public/ 目录的本机 HTTP 服务和 Codex 浏览器进行检查，不访问本地 file:// 页面。

- A/B/C：1440×900 实际截图检查，图片加载成功、没有页面横向溢出。
- A/B/C：320×568 实际截图检查，没有页面横向溢出。B/C 首轮标题出现不佳的落单换行，调整手机字号后在相同尺寸复拍，标题恢复为两行。
- A：390×844 画廊点击“AI 创作调查”后，标题、图片、选中状态同步；方向键切换到屋顶内容成功。
- C：桌面切换语言学习、390×844 切换课堂教学，示例问题、游戏名、三个地点与说明一起更新。仅预设内容切换，不请求 AI。
- B：390×844 未来展望检查。未来章节标题和示例文案经调整后复拍。
- 比较页：390px 宽度无横向溢出，三个预览均加载；可切到 C 与手机布局。窄于390px的可用区域让预览随容器收窄，不声称此时 iframe 仍精确390px。
- 构建和脚本语法检查通过；dist/ 四个 HTML 页面分别通过结构、唯一 ID、锚点和相对资源路径检查。

截图通过本任务工具实际查看，未保存为磁盘图片。不把浏览器模拟尺寸称为手机真机测试，也未验证投资人实际理解或偏好。

## 2026-09-18 · A/C 发布与素材流程图

- 用户保留 A/C；B 移到 `design/archive/`，不进入 `dist/`。
- 增加四段流程与两条素材路径；桌面并排、手机纵向，文字标签与颜色共同表达复用/生成。
- 浏览器检查：A 实际 1440×900，无横向溢出；C 实际 390×844，无横向溢出，流程图文本与卡片可读。截图通过浏览器工具检查，未保存到仓库。
- HTMLParser 检查三页相对链接、文件存在性、锚点、重复 ID；JS 语法检查通过。
- 素材生成表述按真实代码核对：异步按需生成照片、复用模板和家具、校验并保存。不把任意角色/世界生成写成已完成。
- 凭据扫描通过。理解效果尚未由新读者复述验证。

## 标准流程图与具体素材示例（2026-09-18 后续修订）

- 按用户参考图改为过程框、菱形判断、条件箭头与汇合线；A/C 均含桌面与手机 SVG。
- 直接引用自有样板截图，展示人物、桌子、地板、生成照片；SVG 内嵌图片确保作为 img 加载时没有外链依赖。
- 浏览器：A 在实际 397px 宽度检查上下文、判断分支、样板素材、照片、汇合节点与详情入口；C 在实际 1107px 检查桌面图及配色，320px 无横向溢出且选择手机图。
- 详情展开可用，图片无加载失败；页面资源与四份 SVG 解析验证通过；UI foundation 静态检查通过。
- 图示只是能力说明，不声称任意素材的自动生成已上线；新读者理解仍未独立验证。

## 透明原素材和组装前后（2026-09-18）

- 以源码导入核对素材来源。sips 确认 table、shelf-and-folder PNG 有 alpha；浏览器灰白预览底清楚透过物件外部，原图不修改。
- A 手机实际 397px 检查独立物件、编号、组装场景；C 桌面实际 1440px 检查三件独立物件 + 生成原照片，并确认前后展示相同物件。
- 组装图里夹子位于搁架顶面，照片位于放大台，四个编号均对应；原始照片不再从界面裁取。
- SVG 图片集中定义并用 use 复用，体积由每张约 4.5MB 降为 2.4MB，无外部素材加载依赖。

## 双语与系统原理（2026-09-18）

- A/C 与比较页均生成中文、英文；构建阻止未翻译汉字（语言入口“中文”除外）。英文图表也翻译，截图保持原游戏界面。
- 浏览器实际检查 A 中文 1440px、A/C 英文 320px、C 英文 390×844：无页面横向溢出。C 手机原理图、多人规划、英文素材图与创作台截图已查看。
- 开门三种情境切换、英文四段样板切换、C 英文语言学习示例均能更新对应内容；语言切换保留 #engine。
- 比较页 320×568 无页面溢出，英文 C 与 Mobile 控件正确更新 iframe。修复移除换行后的英文单词粘连、窄预览导航允许换行、A/C 预览按钮最小宽度 44px。
- 六份 HTML 的相对资源存在性、锚点与唯一 ID 检查通过；八份 SVG 解析通过；英文脚本语法检查通过；UI 静态检查与凭据扫描通过。
- 交互为预设说明示例，无模型调用或存档写入。多人和用户生成完整游戏仍为规划；Prolog 独立服务验证与旧街集成状态明确区分。
- 截图通过工具查看，未保存在仓库；尚未进行独立读者理解测试。

## 独立版本入口（2026-09-18）

- 按用户要求移除 A/C 正文的顶部设计版本栏与比较入口；中英文切换移入主导航。
- A 中文 1440px、C 英文 320×568 截图检查通过；修复英文手机导航隐藏规则误隐藏语言按钮，复验中文切换可用且无横向溢出。

## 落地页 Logo（2026-09-18）

- 复用落地页原始 SVG 至 A/C 中英文页眉、页脚与 favicon，保留各版原有视觉设计。
- 浏览器检查 C 英文桌面、C 英文与 A 中文 320×568 截图：Logo 和语言入口清晰，无横向溢出；页眉页脚图片均加载成功。A 为白色，C 为深色反色。
- 构建及凭据扫描通过；两个品牌 SVG 与落地页原文件字节一致。

## 完整字标排列修正（2026-09-18）

- 页眉与页脚使用落地页相同的图标、字标字体、字重、大小、字距和横排方式；移除品牌组合中的 AI RPG 小字。
- 浏览器确认 Montserrat 800 已加载，页眉与页脚计算样式均为 14px / 800 / 2.52px 字距 / uppercase。
- C 英文桌面、A 中文与 C 英文实际 320×568 截图检查：品牌组合清晰，语言切换入口保留，无横向溢出。
