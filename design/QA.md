# 候选页面检查

2026-09-18，使用只提供 public/ 目录的本机 HTTP 服务和 Codex 浏览器进行检查，不访问本地 file:// 页面。

- A/B/C：1440×900 实际截图检查，图片加载成功、没有页面横向溢出。
- A/B/C：320×568 实际截图检查，没有页面横向溢出。B/C 首轮标题出现不佳的落单换行，调整手机字号后在相同尺寸复拍，标题恢复为两行。
- A：390×844 画廊点击“AI 创作调查”后，标题、图片、选中状态同步；方向键切换到屋顶内容成功。
- C：桌面切换语言学习、390×844 切换课堂教学，示例问题、游戏名、三个地点与说明一起更新。仅预设内容切换，不请求 AI。
- B：390×844 未来展望检查。未来章节标题和示例文案经调整后复拍。
- 比较页：390px 宽度无横向溢出，三个预览均加载；可切到 C 与手机布局。窄于390px的可用区域让预览随容器收窄，不声称此时 iframe 仍精确390px。
- 构建和脚本语法检查通过；dist/ 四个 HTML 页面分别通过结构、唯一 ID、锚点和相对资源路径检查。

截图通过本任务工具实际查看，未保存为磁盘图片。不把浏览器模拟尺寸称为手机真机测试，也未验证投资人实际理解或偏好。

## 2026-09-18 · A/C 发布与素材流程图

- 用户保留 A/C；B 移到 `design/archive/`，不进入 `dist/`。
- 增加四段流程与两条素材路径；桌面并排、手机纵向，文字标签与颜色共同表达复用/生成。
- 浏览器检查：A 实际 1440×900，无横向溢出；C 实际 390×844，无横向溢出，流程图文本与卡片可读。截图通过浏览器工具检查，未保存到仓库。
- HTMLParser 检查三页相对链接、文件存在性、锚点、重复 ID；JS 语法检查通过。
- 素材生成表述按真实代码核对：异步按需生成照片、复用模板和家具、校验并保存。不把任意角色/世界生成写成已完成。
- 凭据扫描通过。理解效果尚未由新读者复述验证。

## 标准流程图与具体素材示例（2026-09-18 后续修订）

- 按用户参考图改为过程框、菱形判断、条件箭头与汇合线；A/C 均含桌面与手机 SVG。
- 直接引用自有样板截图，展示人物、桌子、地板、生成照片；SVG 内嵌图片确保作为 img 加载时没有外链依赖。
- 浏览器：A 在实际 397px 宽度检查上下文、判断分支、样板素材、照片、汇合节点与详情入口；C 在实际 1107px 检查桌面图及配色，320px 无横向溢出且选择手机图。
- 详情展开可用，图片无加载失败；页面资源与四份 SVG 解析验证通过；UI foundation 静态检查通过。
- 图示只是能力说明，不声称任意素材的自动生成已上线；新读者理解仍未独立验证。
